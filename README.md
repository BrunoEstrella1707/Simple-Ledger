# Simple Ledger
This repository implements a simple ledger application made with TypeScript and Node.js
It has features of creating an account and making transactions between them. The transactions have a list of entries, where the values, directions and accounts are specified. 
All transactions must be balanced. A balanced transaction is a transaction where the values with opposite directions sums up to 0.
